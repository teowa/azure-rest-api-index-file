# azure-rest-api-index-file

Compressed index file for https://github.com/Azure/azure-rest-api-specs built by [index tool](https://github.com/magodo/azure-rest-api-index).

The repository and each release contain `index.json.zip`. The uncompressed
`index.json` is generated temporarily and is not stored in Git.
